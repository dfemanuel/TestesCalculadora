"""import unittest
from calculadora import Calculadora

class TestCalculadora(unittest.TestCase):
    def test_soma_0ponto5(self):
        a = Calculadora()
        self.assertEqual(a.soma(0.3, 0.2), 0.5, "Resultado esperado: 0.5")

    def test_soma_6(self):
        a = Calculadora()
        self.assertEqual(a.soma(3.5, 2.5), 6, "Resultado esperado: 6")

    def test_soma_0ponto24(self):
        a = Calculadora()
        self.assertEqual(a.soma(0.2, 0.04), 0.24, "Resultado esperado: 0.24")
    
    def test_soma_0ponto4(self):
        a = Calculadora()
        self.assertEqual(a.soma(0.36, 0.04), 0.4, "Resultado esperado: 0.4")
    
    def test_soma_0ponto72(self):
        a = Calculadora()
        self.assertEqual(a.soma(0.68, 0.04), 0.72, "Resultado esperado: 0.72")"""